#include <iostream>
using namespace std;

class Pants{
        string color;
        int size;
    public:
        void Pants(string, int);
        
        
        string ans_Color (void) {return (color);}
        int ans_Size (void) {return (size);}     
};
